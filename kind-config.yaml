kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: vortex
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 3000
        hostPort: 3000
        protocol: TCP
      # - containerPort: 5432  (Permanent port mapping)
      #   hostPort: 5432
      #   protocol: TCP